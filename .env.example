# Ollama LLM Configuration
OLLAMA_HOST=http://localhost:11434
# Use llama3.1 on CPU; qwen2.5:14b-instruct is much slower without a GPU
OLLAMA_MODEL=llama3.1:latest
OLLAMA_TIMEOUT=300
OLLAMA_NUM_PREDICT=400

# Document Configuration
PDF_PATH=data/pdf/Human Resources Policies & Procedures Manual (ML-HR-01, V.01).docx.pdf
CHROMA_DB_PATH=chroma_storage

# Embeddings Configuration
EMBEDDING_MODEL=intfloat/multilingual-e5-large
EMBEDDING_DEVICE=cpu

# Retrieval Configuration
TOP_K_RESULTS=8
BM25_ENABLE=true

# Generation — limit context sent to LLM (retrieval still uses TOP_K_RESULTS)
LLM_MAX_CHUNKS=3
LLM_MAX_CHARS_PER_CHUNK=1000

# Application
DEBUG=false
